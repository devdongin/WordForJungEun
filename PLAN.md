# 일본어 단어 복습장 (WordForJungeun) 기획안 v3

## 1. 문제 정의
- 정은이 공부할 단어를 한곳에 모으고, 그 범위 안에서만 매일 퀴즈를 받고 싶다.
- 스프레드시트를 AI 채팅에 복붙하면 텍스트로만 들어가 매번 번거롭고 범위 관리가 안 된다.
- 결론: **단어 공급(GitHub 저장소)** 과 **학습/복습(로컬 HTML)** 을 나눈다.

## 2. 전체 구조
```
[단어 수집 스크립트]  --커밋-->  [public GitHub 저장소: words/*.json]
  (Python, 로컬 또는                         |
   GitHub Actions 정기 실행)                  | HTTPS fetch (읽기 전용)
                                             v
                               [단어장.html (정은 PC, 더블클릭 실행)]
                                 - 단어 동기화
                                 - 새 단어 학습 -> 복습 전환
                                 - 학습 진도는 브라우저 IndexedDB
```
- 저장소에는 **단어만** 있다. 누가 뭘 틀렸는지 같은 **진도는 로컬에만** 있다.
- HTML에는 GitHub 토큰을 넣지 않는다. 읽기만 하므로 필요 없다.

## 3. 요구사항
1. 새 단어를 수집해서 저장소에 쌓는다. 앱은 새 단어를 학습시키고, 학습이 끝난 단어는 복습으로 넘긴다.
2. 한국어 뜻이 나오면 일본어를 **객관식**으로 고른다.
3. 일본어가 나오면 한국어 뜻을 **객관식** 또는 **주관식**으로 답한다.
4. HTML 파일이 public GitHub 저장소에서 단어를 받아와 업데이트한다.

## 4. GitHub에서 단어 받아오기 (요구사항 4)
**가능하다.** 확인 결과 아래 두 주소 모두 `Access-Control-Allow-Origin: *` 를 돌려준다. 더블클릭으로 연 HTML 파일에서도 fetch가 된다.

| 방식 | 주소 예시 | 제한 | 용도 |
|---|---|---|---|
| **raw (추천)** | `https://raw.githubusercontent.com/<user>/<repo>/main/words/index.json` | 사실상 없음, 최대 5분 캐시 | 단어 파일 받기 |
| REST API | `https://api.github.com/repos/<user>/<repo>/contents/words` | 비인증 시간당 60회 | 파일 목록, 최신 커밋 확인 |

**동기화 흐름**
1. 앱 시작 시 또는 "단어 업데이트" 버튼 → `words/index.json` 받기.
2. index에 적힌 각 단어 파일의 `version` 이 로컬과 다르면 그 파일만 다시 받기.
3. 단어 `id` 기준으로 병합. 새 id는 "새 단어"로 추가, 기존 id는 뜻/예문만 갱신하고 **진도는 유지**.
4. 저장소에서 삭제된 단어는 로컬에서 숨김 처리 (진도는 남김).
5. 오프라인이면 마지막으로 받은 단어로 그대로 동작.

**주의**
- 저장소가 public이면 단어 목록은 누구나 볼 수 있다. 진도와 개인 메모는 올라가지 않는다.
- 앱에서 저장소로 **쓰기**(진도 업로드)는 토큰이 필요해서 이번 범위에서 뺀다.

### 저장소 파일 형식
```
words/
  index.json          # 파일 목록과 버전
  n5.json
  n4.json
  2026-09.json        # 월별 수집분
```
`index.json`
```json
{ "updatedAt": "2026-09-17", "files": [ { "path": "n5.json", "version": 1, "count": 50 } ] }
```
- `path` 는 `words/` 기준 상대 경로. 단어 파일을 고치면 `version` 을 올려야 앱이 다시 받는다.

단어 파일
```json
[
  {
    "id": "jp-勉強-べんきょう",
    "kanji": "勉強",
    "reading": "べんきょう",
    "meanings": ["공부", "학습"],
    "pos": "명사·する동사",
    "example": { "jp": "毎日日本語を勉強します。", "ko": "매일 일본어를 공부합니다." },
    "tags": ["N5"],
    "source": "JMdict"
  }
]
```
- `id` 는 표기+읽기로 만들어 절대 바뀌지 않게 한다. 진도 연결의 기준이다.
- `meanings` 는 배열. 주관식 채점과 객관식 오답 생성에 쓴다.

## 5. 새 단어 수집 (요구사항 1의 앞부분)
사이트를 무작정 크롤링하면 저작권과 이용약관 문제가 생기고 사이트 구조가 바뀌면 깨진다. **공개 라이선스 데이터를 받아 가공**하는 방식을 기본으로 한다.

| 출처 | 내용 | 라이선스 |
|---|---|---|
| JMdict / JMdict-simplified | 일본어 표기, 읽기, 품사, 영어 뜻 | CC BY-SA |
| 오픈 JLPT 단어 목록 | 급수 태그 | 목록마다 확인 필요 |
| 정은이 직접 입력한 단어 | 교재, 드라마에서 본 단어 | 본인 |

**수집 스크립트 (`scripts/collect.py`)**
1. 급수별 또는 주제별로 새 단어 후보를 고른다. 이미 저장소에 있는 id는 제외.
2. JMdict에는 한국어 뜻이 거의 없으므로 **Claude API로 한국어 뜻과 예문을 생성**한다.
3. 결과를 사람이 한 번 훑어볼 수 있게 PR 또는 검토용 CSV로 만든다.
4. 확정분을 `words/*.json` 에 추가하고 `index.json` 버전을 올린다.

실행 위치는 두 가지 중 하나.
- **로컬 수동 실행**: API 키가 내 PC에만 있다. 가장 단순.
- **GitHub Actions 주 1회**: API 키를 저장소 Secret에 둔다. 자동으로 PR 생성 후 내가 머지.

정은이 스프레드시트로 정리한 단어도 같은 스크립트로 CSV를 읽어 저장소에 넣는다.

## 6. 학습 흐름 (요구사항 1의 뒷부분)
단어마다 상태가 있다.

```
[새 단어] --오늘 학습 선택--> [학습 중] --연속 정답 기준 통과--> [복습] --간격 반복-->
                                ^                                  |
                                +------ 복습에서 연속 2회 오답 -------+
```

| 상태 | 설명 | 나오는 방식 |
|---|---|---|
| 새 단어 | 동기화로 들어왔지만 아직 안 본 단어 | 하루 신규 한도만큼 학습으로 투입 |
| 학습 중 | 오늘 처음 익히는 단어 | 카드 보기 → 바로 퀴즈, 틀리면 세션 안에서 다시 나옴 |
| 복습 | 학습을 통과한 단어 | 복습 예정일이 된 날 퀴즈에 나옴 |
| 보류 | 사용자가 "이미 앎" 또는 "제외" 처리 | 안 나옴 |

**학습 → 복습 전환 기준**
- 한 세션에서 **양방향 문제(한→일, 일→한)를 각각 1회 이상 맞히고, 마지막 2회 연속 정답**이면 복습으로 넘긴다.
- 세션 안에 통과 못 하면 다음날 학습 중으로 다시 나온다.

**복습 간격**
- 정답: 1 → 3 → 7 → 14 → 30 → 60일.
- 오답: 간격을 1일로 되돌린다. 복습에서 연속 2회 오답이면 학습 중으로 강등.

**하루 세션 구성**
1. 복습 예정 단어 먼저 (상한 설정 가능, 기본 30).
2. 새 단어 학습 (기본 하루 10개).
3. 끝나면 결과 화면과 틀린 단어 다시 풀기.

## 7. 퀴즈 유형 (요구사항 2, 3)
### A. 한국어 → 일본어 객관식
```
"공부"
① 勉強 (べんきょう)   ② 勤務 (きんむ)   ③ 研究 (けんきゅう)   ④ 教育 (きょういく)
```
- 보기는 한자와 읽기를 같이 표시. 설정으로 읽기 숨기기 가능.
- 뜻이 여러 개면 대표 뜻 1개만 보여주고, 나머지는 정답 확인 후 표시.

### B. 일본어 → 한국어 객관식
```
勉強 (べんきょう)
① 공부   ② 근무   ③ 연구   ④ 교육
```
- 읽기는 기본 숨김, "읽기 보기" 버튼. 설정에서 항상 표시로 변경 가능.

### C. 일본어 → 한국어 주관식
```
勉強 (べんきょう)
[ 뜻 입력 ______ ]
```
채점 규칙
1. 공백, 문장부호 제거 후 `meanings` 중 하나와 일치하면 정답.
2. 어미 흔들림 허용: "공부하다", "공부함" → "공부"로 정규화.
3. 오타는 **자동으로 정답 처리**한다. 한글을 자모로 분해한 뒤 편집 거리로 판단한다.
   - 뜻이 2글자 이하: 오타 허용 안 함 (예: "공부"와 "공무"는 자모 1개 차이지만 다른 단어).
   - 3~5글자: 자모 1개 차이까지 정답 (예: "도서관" ← "도서곤").
   - 6글자 이상: 자모 2개 차이까지 정답.
   - 입력이 다른 단어의 뜻과 정확히 같거나 더 가까우면 오타가 아니라 오답으로 처리.
   - 정답 화면에 "오타 인정: 입력값 → 정답" 을 작게 표시해 올바른 철자를 확인시킨다.
4. 그래도 애매하면 "내 답도 맞음" 버튼으로 수동 인정. 인정한 답은 그 단어의 추가 정답으로 로컬에 저장.

### 오답 보기 만드는 법
- 같은 품사, 같은 급수 태그에서 우선 고른다.
- 뜻이 겹치는 단어는 보기에서 제외 (예: "공부"와 "학습").
- 학습 중 단어끼리 섞이면 헷갈리기 좋으니 우선 사용.

### 유형 배분
- 학습 중: A와 B를 번갈아 출제해 양방향을 모두 확인.
- 복습: 기본 A 40%, B 30%, C 30%. 설정에서 C 사용 여부와 비율 조정.

## 8. 로컬 데이터 (IndexedDB)
| 저장소 | 내용 |
|---|---|
| words | 저장소에서 받은 단어 캐시 + 직접 추가 단어 |
| progress | wordId, 상태, 복습 간격, 다음 복습일, 연속 정답/오답, 추가 인정 답 |
| logs | 날짜, wordId, 문제 유형, 정오, 걸린 시간 |
| meta | 마지막 동기화 시각, 파일별 version, 설정 |

- **JSON 백업 내보내기/가져오기**는 필수. 브라우저 데이터가 지워지면 진도가 사라지기 때문.

## 9. 화면 구성
1. **홈**: 오늘 복습 수, 새 단어 수, 연속 학습 일수, "시작" 버튼, 마지막 동기화 시각.
2. **학습 카드**: 한자, 읽기, 뜻, 예문을 보여주고 "익혔어요" → 바로 퀴즈.
3. **퀴즈**: 한 문제씩, 진행 바, 즉시 정오 피드백과 정답 카드.
4. **결과**: 점수, 복습으로 넘어간 단어, 틀린 단어 다시 풀기.
5. **단어장**: 상태별 필터(새 단어, 학습 중, 복습, 보류), 검색, 직접 추가.
6. **설정**: 저장소 주소, 하루 신규 수, 복습 상한, 유형 비율, 읽기 표시, 백업.

## 10. 기술 스택
| 구성 | 선택 |
|---|---|
| 앱 | 단일 `단어장.html`, 순수 HTML + JS, 외부 라이브러리 없음 |
| 저장 | IndexedDB |
| 단어 공급 | public GitHub 저장소 + raw.githubusercontent.com |
| 수집 | Python 스크립트, 로컬 또는 GitHub Actions |
| AI | 수집 단계에서만 Claude API 사용. 앱은 AI 없이 동작 |

앱에 AI를 넣지 않는 이유: 뜻과 예문을 미리 만들어 저장소에 넣으면 앱에 API 키가 필요 없고 퀴즈가 즉시 뜬다.

## 11. 로드맵
| 단계 | 내용 |
|---|---|
| 1 | 저장소 단어 형식 확정 + 샘플 단어 50개 + HTML에서 동기화, 단어장 화면 |
| 2 | 퀴즈 A, B + 결과 화면 |
| 3 | 새 단어 → 학습 중 → 복습 상태 전환 + 간격 반복 |
| 4 | 퀴즈 C 주관식 채점 + 백업 |
| 5 | 수집 스크립트 (JMdict 가공 + Claude로 한국어 뜻 생성) + GitHub Actions |

## 12. 결정 사항
### 저장소: 이 저장소 하나에 모두 둔다
```
WordForJungeun/
  app/단어장.html
  words/index.json, n5.json, n4.json, custom.json
  scripts/collect.py, import_csv.py
  PLAN.md
```
- 사용자가 한 명이라 저장소를 나눠도 얻는 게 없다. 관리할 곳만 늘어난다.
- 앱과 단어 형식이 같이 바뀌므로 한 커밋에서 함께 고칠 수 있다.
- 앱의 저장소 주소 기본값을 이 저장소로 박아두면 정은은 설정할 게 없다.
- 나중에 폰에서 쓰고 싶어지면 이 저장소에 GitHub Pages만 켜면 된다.

### 수집 순서: JLPT 급수를 뼈대로, 교재 단어를 우선 끼워 넣는다
- 급수 목록은 한 번 만들어두면 새 단어가 끊기지 않는다. 교재만 따르면 매번 직접 입력해야 한다.
- 정은이 교재나 드라마에서 본 단어는 CSV로 `words/custom.json` 에 넣고, 앱은 새 단어를 뽑을 때 **custom을 급수 목록보다 먼저** 투입한다.
- 시작 급수는 정은 수준에 맞춘다. 모르면 N5부터 시작하고, 학습 카드의 "이미 앎" 버튼으로 아는 단어를 빠르게 넘긴다.
- 급수 목록 안에서는 빈도순으로 투입한다.

### 시작 급수: N5

## 13. 진행 상황
- 1단계 완료 (2026-09-17): `app/단어장.html`, `words/index.json`, `words/n5.json` 샘플 50개, `words/custom.json`.
  - 단어 동기화, 상태 구분(새 단어/학습 중/복습/보류), 단어장 검색과 필터, 직접 추가와 붙여넣기, "이미 앎", 백업.
  - 상태 전환 규칙은 3단계에서 퀴즈와 함께 구현.
- 로컬 확인: `python -m http.server 8765` 후 `http://localhost:8765/app/단어장.html` (이때는 로컬 `words/` 를 읽음).
